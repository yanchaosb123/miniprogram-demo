<!--pages/poems/poems.wxml-->
<view class = 'group' >
  <block wx:for ="{{bookCategory}}" wx:key="id"  >
    <text id="{{item.id}}" bindtap="getBookChildCategory" class="parent" >{{item.description}} </text>
  </block>
</view>
